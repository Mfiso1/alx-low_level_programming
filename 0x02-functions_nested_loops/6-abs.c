#include "main.h"
/**
 * _abs - computes the absolute value of an interger
 * @n: interger returned must be an absolute value
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
