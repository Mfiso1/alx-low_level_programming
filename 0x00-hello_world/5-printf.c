#include <stdio.h>
/**
 * main - The programme uses printf function to output a message
 * Return: 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
